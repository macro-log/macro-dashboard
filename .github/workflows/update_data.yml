name: Financial Data Update
on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install yfinance pandas google-generativeai requests deep-translator yfinance pandas

      - name: Run Update Script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python << 'EOF'
          import yfinance as yf
          import json
          import os
          import requests
          import re
          from xml.etree import ElementTree
          from deep_translator import GoogleTranslator
          import google.generativeai as genai

          api_key = os.getenv("GEMINI_API_KEY")
          genai.configure(api_key=api_key)
          translator = GoogleTranslator(source='auto', target='ko')

          def get_score(val, low, high, reverse=False):
              score = ((val - low) / (high - low)) * 100
              return max(0, min(100, 100 - score if reverse else score))

          # 1. 🚨 [수정 완료] 7대 엔진 점수 100% 반영 🚨
          try:
              tickers = {"VIX": "^VIX", "SPY": "SPY", "HYG": "HYG", "IEF": "IEF", "UUP": "UUP", "QQQ": "QQQ", "DIA": "DIA", "GOLD": "GC=F", "BTC": "BTC-USD"}
              raw_data = {k: yf.Ticker(v).history(period="150d") for k, v in tickers.items()}

              s1 = get_score(raw_data["VIX"]['Close'].iloc[-1], 12, 35, reverse=True)
              s2 = get_score(raw_data["SPY"]['Close'].iloc[-1] / raw_data["SPY"]['Close'].rolling(window=125).mean().iloc[-1], 0.9, 1.1)
              s3 = get_score(raw_data["HYG"]['Close'].iloc[-1] / raw_data["IEF"]['Close'].iloc[-1], 0.7, 0.9)
              s4 = get_score(raw_data["UUP"]['Close'].iloc[-1] / raw_data["UUP"]['Close'].rolling(window=125).mean().iloc[-1], 0.95, 1.1, reverse=True)
              s5 = get_score(raw_data["QQQ"]['Close'].iloc[-1] / raw_data["DIA"]['Close'].iloc[-1], 1.0, 1.3) # 누락됐던 S5 복구!
              s6 = get_score(raw_data["GOLD"]['Close'].iloc[-1] / raw_data["GOLD"]['Close'].rolling(window=125).mean().iloc[-1], 0.95, 1.15, reverse=True) # 누락됐던 S6 복구!
              s7 = get_score(raw_data["BTC"]['Close'].iloc[-1] / raw_data["BTC"]['Close'].rolling(window=20).mean().iloc[-1], 0.8, 1.2)
              
              # 이제 진짜 100% 비율 맞습니다.
              final_score = int((s1*0.20) + (s2*0.20) + (s3*0.20) + (s4*0.15) + (s5*0.15) + (s6*0.05) + (s7*0.05))

              # 하단 리스트에 뿌려줄 7대 엔진 상세 데이터
              engine_data = [
                  {"word": "시장 공포(VIX)", "display_score": f"{int(s1)}점", "count": "Market", "sentiment_score": round((s1-50)/5, 1)},
                  {"word": "가격 추세(SPY)", "display_score": f"{int(s2)}점", "count": "Price", "sentiment_score": round((s2-50)/5, 1)},
                  {"word": "기업 신용(HYG)", "display_score": f"{int(s3)}점", "count": "Credit", "sentiment_score": round((s3-50)/5, 1)},
                  {"word": "달러 강세(UUP)", "display_score": f"{int(s4)}점", "count": "Cash", "sentiment_score": round((s4-50)/5, 1)},
                  {"word": "기술주 선호(QQQ)", "display_score": f"{int(s5)}점", "count": "Tech", "sentiment_score": round((s5-50)/5, 1)},
                  {"word": "안전자산(GOLD)", "display_score": f"{int(s6)}점", "count": "Safe", "sentiment_score": round((s6-50)/5, 1)},
                  {"word": "코인 열기(BTC)", "display_score": f"{int(s7)}점", "count": "Crypto", "sentiment_score": round((s7-50)/5, 1)}
              ]
          except Exception as e:
              final_score, engine_data = 50, []

          # 2. 형님의 오리지널 연준 심리 데이터 (차트용)
          fomc_data = [
              {"word": "INFLATION", "sentiment_score": -1.8, "status": "NEUTRAL", "display_score": "-1.8", "count": 52},
              {"word": "RATE", "sentiment_score": 0.4, "status": "NEUTRAL", "display_score": "+0.4", "count": 48},
              {"word": "POLICY", "sentiment_score": -0.2, "status": "NEUTRAL", "display_score": "-0.2", "count": 34},
              {"word": "RISKS", "sentiment_score": -0.5, "status": "NEUTRAL", "display_score": "-0.5", "count": 21},
              {"word": "GROWTH", "sentiment_score": -0.1, "status": "NEUTRAL", "display_score": "-0.1", "count": 20},
              {"word": "LABOR", "sentiment_score": -0.0, "status": "NEUTRAL", "display_score": "-0.0", "count": 18},
              {"word": "CREDIT", "sentiment_score": 0.2, "status": "NEUTRAL", "display_score": "+0.2", "count": 13},
              {"word": "GDP", "sentiment_score": -0.2, "status": "NEUTRAL", "display_score": "-0.2", "count": 11},
              {"word": "PRICES", "sentiment_score": 0.0, "status": "NEUTRAL", "display_score": "0.0", "count": 11},
              {"word": "EMPLOYMENT", "sentiment_score": -0.1, "status": "NEUTRAL", "display_score": "-0.1", "count": 7},
              {"word": "ECONOMY", "sentiment_score": -0.7, "status": "NEUTRAL", "display_score": "-0.7", "count": 5},
              {"word": "STABILITY", "sentiment_score": 0.0, "status": "NEUTRAL", "display_score": "0.0", "count": 3}
          ]

          # 3. 뉴스 제목 + 내용(Description) 번역
          sectors = {
              "BIG TECH": "https://news.google.com/rss/search?q=Apple+Nvidia+Microsoft+Stock&hl=en-US&gl=US&ceid=US:en",
              "SEMICON": "https://news.google.com/rss/search?q=Semiconductor+TSMC+ASML&hl=en-US&gl=US&ceid=US:en",
              "FINANCE": "https://news.google.com/rss/search?q=JPMorgan+Goldman+Sachs+FED&hl=en-US&gl=US&ceid=US:en"
          }
          sector_news_data = {}
          for name, url in sectors.items():
              try:
                  response = requests.get(url, timeout=10)
                  tree = ElementTree.fromstring(response.content)
                  items = []
                  for item in tree.findall('.//item')[:3]:
                      en_title = item.find('title').text
                      en_desc_raw = item.find('description').text if item.find('description') is not None else ""
                      en_desc = re.sub('<[^<]+>', '', en_desc_raw)
                      
                      ko_title = translator.translate(en_title)
                      ko_desc = translator.translate(en_desc[:120]) + "..." if en_desc else ""
                      display_text = f"{ko_title}<br><span style='font-size:11px; color:#94a3b8; font-weight:normal; display:block; margin-top:4px;'>{ko_desc}</span>"
                      
                      items.append({"title": display_text, "link": item.find('link').text, "date": item.find('pubDate').text[:16]})
                  sector_news_data[name] = items
              except Exception as e:
                  sector_news_data[name] = []

          # 4. AI 브리핑
          ai_summary = "데이터 분석 완료."
          try:
              top_titles = [n['title'] for n in sector_news_data.get("BIG TECH", [])[:2] + sector_news_data.get("FINANCE", [])[:2]]
              news_text = "\n".join([re.sub('<[^<]+>', '', t) for t in top_titles])
              prompt = f"현재 시장 점수 {final_score}/100점. 핵심 뉴스:\n{news_text}\n이 뉴스를 바탕으로 현재 시장 상황을 한국어로 3줄 요약해줘. 형님께 보고하듯 씩씩하고 위트 있게!"
              response = genai.GenerativeModel('gemini-1.5-flash').generate_content(prompt)
              ai_summary = response.text
          except: pass

          # 5. 저장 (주머니 분리!)
          os.makedirs('PROJECT', exist_ok=True)
          data = {
              "master_score": final_score,
              "market_temp": "-2.5",             # 연준 온도 (고정값)
              "ai_summary": ai_summary,
              "fomc_indicators": fomc_data,      # 차트용
              "engine_indicators": engine_data,  # 리스트용
              "sector_news": sector_news_data
          }
          
          with open('PROJECT/indicators.json', 'w', encoding='utf-8') as f:
              json.dump(data, f, ensure_ascii=False, indent=4)
          EOF

      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "7대엔진 정상화 및 데이터 주머니 분리 🚀" || echo "No changes"
          git push# ==========================================
# 🚀 [추가 탑재] 빅테크 실적발표 자동 수집 파이프라인 (yfinance)
# ==========================================
import os
import pandas as pd
import yfinance as yf

print("=== 빅테크 실적발표 자동 업데이트 시작 ===")
events_file = 'PROJECT/economic_events.json'

# 1. 기존 달력 파일 읽기 (형님의 수동 MACRO 일정 보호)
if os.path.exists(events_file):
    with open(events_file, 'r', encoding='utf-8') as f:
        try:
            events_data = json.load(f)
        except:
            events_data = []
else:
    events_data = []

# 2. 형님이 수동으로 넣은 '지표(MACRO)' 일정만 살리고, 기존 실적 데이터는 싹 비움
manual_events = [e for e in events_data if e.get('type') != 'EARNINGS']
auto_earnings = []

# 3. 핵심 타겟 빅테크 종목 (원하시면 여기 티커를 추가/수정하셔도 됩니다!)
tickers = ['AAPL', 'MSFT', 'NVDA', 'TSLA', 'AMZN', 'GOOGL', 'META']

now_utc = pd.Timestamp.now('UTC')

for ticker in tickers:
    try:
        tk = yf.Ticker(ticker)
        # 야후 파이낸스 DB에서 다이렉트로 향후 실적발표일 가져오기
        earning_dates = tk.get_earnings_dates(limit=10)
        
        if earning_dates is not None and not earning_dates.empty:
            # 현재 시간 이후의 다가올 미래 데이터만 필터링
            future_dates = earning_dates[earning_dates.index >= now_utc]
            
            if not future_dates.empty:
                # 가장 가까운 미래의 실적발표일 추출
                next_date = future_dates.index.min()
                date_str = next_date.strftime('%Y-%m-%d')
                
                auto_earnings.append({
                    "date": date_str,
                    "name": f"{ticker} 실적발표",
                    "importance": "HIGH",
                    "type": "EARNINGS"
                })
                print(f"[성공] {ticker} 실적발표일: {date_str}")
    except Exception as e:
        print(f"[실패] {ticker} 실적 파싱 에러: {e}")

# 4. 수동 지표 + 자동 실적 합치고 날짜순 정렬
final_events = manual_events + auto_earnings
final_events.sort(key=lambda x: x['date'])

# 5. 캘린더 파일 덮어쓰기 (자동 완성)
with open(events_file, 'w', encoding='utf-8') as f:
    json.dump(final_events, f, ensure_ascii=False, indent=4)
    
print("=== 빅테크 실적발표 자동 업데이트 완료 ===")